{
  "name": "amd.ts",
  "version": "1.1.0",
  "description": "A specialized AMD implementation meant to support modules generated by the TypeScript compiler.",
  "main": "./build/amd.js",
  "scripts": {
    "build": "tsc -p tsconfig.release.json && node $npm_package_main && echo Build succeeded.\n",
    "clean": "rm -Rf ./build",
    "minify": "uglifyjs --compress --mangle --output build/amd.min.js -- $npm_package_main",
    "prepublish": "npm run test && npm run clean && npm run build && npm run minify",
    "test": "tsc && node build/test.js && echo All tests passed.\n"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/federico-lox/amd.ts.git"
  },
  "keywords": [
    "typescript",
    "amd",
    "javascript",
    "module",
    "bundle"
  ],
  "author": "Federico \"Lox\" Lucignano",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/federico-lox/amd.ts/issues"
  },
  "homepage": "https://github.com/federico-lox/amd.ts#amdts--",
  "devDependencies": {
    "typescript": "1.8.10",
    "uglify-js": "2.6.2"
  }
}
